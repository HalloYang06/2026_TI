# 钢球控制调试复盘与必坑清单

适用范围：本仓库的 TI MSPM0 底盘、EdgeTalk M33、树莓派视觉链路；Q3 至 Q6。

这不是参数说明书，而是一次次现场现象反推根因后留下的排障顺序。调车先看证据，再动一个变量；不要把“能动”和“控制正确”混为一谈。

## 当前已烧录基线

以下是截至 2026-08-07 的编译默认值。树莓派运行时下发的值只在 RAM 中有效，M33 重启后恢复本表。

| 项目 | 当前值 | 位置 |
| --- | ---: | --- |
| Q3 调平角 | 1.7500 rad | `hball_bench_app.c` |
| Q3 `Kp / Ki / Kd` | `0.80 / 0.16 / 0.16` | `hball_bench_app.c` |
| Q3 目标扫动速度 | 20 cm/s | `hball_bench_app.c` |
| Q4 `Kp / Kv / Ki` | `2.20 / 1.60 / 0.05` | `hball_bench_app.c` |
| Q5 `Kp / Kv / Ki` | `2.20 / 1.60 / 0.05` | `hball_bench_app.c` |
| Q6 `Kp / Kv / Ki` | `2.50 / 1.15 / 0.18` | `hball_bench_app.c` |
| 纵向加速度前馈 | 0.55 | `hball_deployment_controller.c` |
| 横向加速度耦合 | Q4/Q5: 0.25；Q6: 0.50 | `hball_bench_app.c` |
| Q4/Q5 软启动 | 1.20 s | `main.c` |
| Q6 软启动 | 0 点 1.20 s；随目标位置延长，最大 1.80 s | `main.c` |

旧的 Q3 复盘文档中出现过 `0.70 / 0.15 / 0.35`、`1.721 rad` 等历史试验值；不要再把它们当作当前烧录参数。

## 我实际使用的定位顺序

1. 先确认任务和链路状态：LCD 的任务号、M33 `READY/LINK/IMU`、电机是否已完成 B21 调平。没有 READY 时，不调 PID。

2. 只采一轮，保存完整日志：记录任务模式、视觉位置、管角、IMU、底盘速度/阶段、饱和标志和时间戳。先确认这一轮确实是 Q3/Q4/Q5/Q6，旧帧和错误模式的数据一律作废。

3. 先分域：启动不了属于状态机/传感器有效性；位置认错属于视觉；有输出但角度不动属于电机/CAN；能动但误差不收敛才属于控制器。

4. 看“控制要求”和“执行能力”是否一致。若管角已经顶在 ±2°，继续增大前馈或 Kp 没有意义；必须先承认是饱和、软启动或目标轨迹的问题。

5. 每次只改一个因子：例如只改 Q6 横向耦合，或只改 Q3 `Kd`。一次同时改前馈、滤波、角度限幅，会不知道变好或变坏是哪个造成的。

6. 每一小步都编译、烧录、跑一轮并提交。结果要量化为：最大误差、首次进入 ±1 cm 的时间、稳态偏差、最大超调、IMU 丢帧和状态机重试次数。

## 现象到根因速查

| 现场现象 | 首先看的证据 | 已定位根因/处理 | 最容易犯的错 |
| --- | --- | --- | --- |
| `MISS: IMU`，SW1 要按多次才启动 | IMU 帧计数、队列 high-water、dropped、MISS 时间 | 5 ms 内约 58 B 输入，旧服务一次只取 32 B；LCD 初始化前已开始接收，256 B 队列堆积溢出。已改为 1024 B 队列、阻塞后一次服务 256 B（解析仍按 32 B）、任务栈 4 KiB，并把 WIT 启动移到 LCD 阻塞初始化后 | 只放宽超时或一直重按 SW1；这会掩盖溢出，不会消除根因 |
| SW1 按下无动作、LCD 非 READY | 当时是否 IMU/视觉有效、任务 epoch、M33 是否收到 PREPARE/START | 启动门本来就要求关键输入有效；IMU MISS 时 M33 不会 READY。状态机用 epoch 防止旧 START/STOP 帧串入新任务 | 为了“按一次就动”删掉 READY 门，导致带失效传感器运动 |
| Q3 按键后球直接冲边或停在 2–3 cm | B21 是否先完成；电机机械角/速度是否进入调平收敛窗；视觉零点 | Q3 依赖预调平：B21 将管道缓慢回到调平角，稳定后再人工把球放 O 点，再按 SW1。每次重新摆放不能假设初始角仍正确 | 把“初始管角错”当成 PID 坏；或者手动放球后使用了旧视觉帧 |
| 球在中间却报告 ±11 cm | 摄像头原始画面、视觉置信度/连续性、反光区域 | 光影/高光会被误识别为球。先修视觉判定或剔除异常帧，再谈 PID | 用错误位置去加大 Kp，实际会把球打向边界 |
| Q3 到 -5 cm 后回弹到 -6 cm，再回来 | 目标速度、误差速度、管角限幅、最后阶段积分 | 这是末段制动/阻尼问题，优先看 `Kd` 和目标减速，不是直接抬 `Kp`。最终默认值为 `Kp=.80, Ki=.16, Kd=.16, rate=20 cm/s` | 认为三项“都没用”，一次同时修改三项，失去可比性 |
| Q4/Q6 总是差约 2 cm，永远够不着目标 | 稳态时管角是否已饱和、积分是否增长、球速是否接近零 | 属于静态偏置/积分/可用管角，而不是起步瞬态。先看限幅，再小幅调该任务自己的 `Ki` 或平衡角 | 用全局纵向加速度前馈补静态误差；前馈只应补动态扰动 |
| Q4/Q5/Q6 刚启动瞬间偏到约 -2.8 cm | 起步时管角限幅是否跳变、底盘软启动曲线、IMU bias 时刻 | 先前的“首秒 2.4°、随后 2°”造成了限幅突变；已统一正式限幅。IMU bias 现在在底盘开始运动前锁定 | 只调 PID，忽略执行器限幅和底盘速度曲线的跳变 |
| 出弯时 Q6 偏差变大 | X/Y 加速度、管角是否饱和、球速度、轨迹阶段 | 转弯不是只看 X；纵向与横向加速度都要进模型。Q6 横向耦合目前为 0.50，Q4/Q5 为 0.25 | 只看单轴，或同时把耦合、滤波时间和管角上限都改大。此前这种组合在实车上变差，已撤回 |
| 电机像舵机一样慢、命令后半拍才动 | CAN 发送周期/成功数、位置回读新鲜度、CSP 状态、管角实际值 | 控制周期和发命令周期均应按 2 ms 观察；先证明命令已发、回读新鲜、机械没有进入保护/限位 | 仅凭肉眼说“PID 太慢”，没有分清通信延迟、速度限幅和控制量不足 |
| TI 无响应、SW3 也无效 | 调试器是否 halt、复位后是否 `go`、供电和 watchdog 日志 | J-Link/pyOCD 检查寄存器时可能让核停在 halt；必须恢复运行。也要区分固件卡死和调试器主动暂停 | 看到屏幕不刷新就直接重新烧录，丢失现场状态 |

## IMU 丢失：完整因果链

`WIT UART 输入速率 > 单周期处理量`，因此 RX 队列周期性积压；开始阶段 LCD 的阻塞初始化又放大首段积压。队列溢出后，解析器丢失字节边界，连续帧不可用，M33 的 IMU 有效位清掉，任务启动门拒绝 SW1。现场表现因此是“约一段时间一次 MISS，随后一段时间恢复”，不是 IMU 物理掉电。

修复后要同时确认：`dropped=0`、队列 high-water 没有贴近 1024、IMU 帧计数持续增长、M33 READY 正常。只看到某一帧 IMU 数据不等于链路已经稳定。

详细原始证据见 [IMU 丢失根因文档](imu-dropout-root-cause-and-fix-20260801.md) 和 [MISS/SW1 分析](imu-miss-sw1-debug-analysis-20260801.md)。

## 树莓派运行时下发：能改什么，不能改什么

命令格式：

```bash
python3 /home/halloyang/hball-edgetalk-camera/edgetalk_tune.py <name> <value>
```

| 名称 | 范围/含义 | 何时生效 |
| --- | --- | --- |
| `q3_kp` | 0.3–1.2 | 空闲时，下一轮 Q3 |
| `q3_ki` | 0–0.6 | 空闲时，下一轮 Q3 |
| `q3_kd` | 0.15–0.6 | 空闲时，下一轮 Q3 |
| `q3_rate_cms` | 5–30 cm/s | 空闲时，下一轮 Q3 |
| `q3_level_mrad` | 1400–1900 mrad | 空闲时，下一轮调平/保持基准 |
| `run_deg` | 正式运行管角限幅 | 空闲时 |
| `run_recovery_deg` | 恢复阶段管角限幅 | 空闲时 |
| `settle_deg` | 稳态管角限幅 | 空闲时 |
| `settle_capture_deg` | 捕获阶段限幅 | 空闲时 |
| `settle_recovery_deg` | 稳态恢复限幅 | 空闲时 |
| `kp`、`kv`、`ki` | 通用 LQI 增益 | 空闲时，但见下条陷阱 |

**关键陷阱：**`kp`、`kv`、`ki` 可以成功下发，却会在正式 Q4/Q5/Q6 启动时被 `hball_apply_mission_gains()` 的任务专属编译常量覆盖。因此它们不能用来稳定地调 Q4/Q5/Q6；目前没有 `q4_kp`、`q5_kp`、`q6_kp` 这类树莓派运行时变量。要永久修改 Q4/Q5/Q6，改 M33 源码中的对应 `HBALL_BALL_Q*_K*` 后重新编译烧录。

所有上述下发参数均为 RAM 值；M33 重启会回到“当前已烧录基线”。调出好结果后必须写回源码、编译、烧录、再做冷启动复测。

## Q3 和 Q4–Q6 不可混用的部分

Q3 是管道内的视觉位置 PID 扫动题：`Kp/Ki/Kd + 目标速度` 是主调节量，且需要 B21 预调平和人工放 O 点。

Q4–Q6 是行驶时的 LQI/状态估计控制：核心是位置、速度、积分、IMU 纵横向加速度补偿、底盘启动/转弯轨迹与管角饱和。不要把 Q3 的 `Kd` 结论照搬到 Q4–Q6，也不要把 Q4–Q6 的加速度前馈套到 Q3。

## 对数值日志的最低要求

每段试跑至少记录并检查：

- 任务 `mode` 与 epoch：确保数据属于刚启动的题目，而非旧帧。
- 视觉位置 `x`、目标 `r`、误差 `e=r-x`、球速度：判断是稳态偏置、欠阻尼还是视觉跳变。
- 管道命令角和实际机械角：判断是否饱和或传动/通信没跟上。
- X/Y 加速度和滤波后值：转弯时必须两轴一起看。
- IMU/视觉有效位、UART dropped、队列 high-water、帧计数：先排链路再调控制。
- 底盘软启动阶段、实际速度：把起步尖峰与控制器本身分离。

HBLG 转 CSV 的工具：

```bash
python3 vision/raspberrypi/control_log_to_csv.py input.hblg output.csv --all-sources
```

不要在服务正运行时抢同一 USB 源采集；先停用户服务，单独采集，再恢复服务。否则“日志缺帧”可能只是两个进程抢设备。

## 编译、烧录与复核必坑

1. TI 固件常规构建从 `firmware/mspm0/wit-oled-hardware-spi` 执行 `cmd.exe /d /c build-keil.bat`。旧的 DAPLink 脚本依赖缺失的 `tools/ti-env.bat` 时会失败；已验证可用 pyOCD 或 J-Link 直接烧录。

2. J-Link 烧录后必须确认 `Program/Verify` 成功，并让目标 `go`。仅连接/复位不代表程序正在跑。

3. EdgeTalk M33 与 TI 是两份固件；改 M33 控制器只烧 TI 不会生效，反之亦然。每次变更前先写清“这一行代码属于哪一颗 MCU”。

4. 侧边任务也在改固件时，先 `git status --short`，不要覆盖或暂存无关改动。一次物理试验对应一个小提交，才能退回最后已知可用的参数。

5. 烧录完成不等于验证完成。至少观察一轮：M33 READY、IMU 帧连续、视觉位置可信、按一次 SW1 可以进入正确任务、机械角没有卡在限位。

## 一页现场流程

```text
上电
  -> 看 IMU/视觉/Link 都为 READY
  -> B21 调平（Q3/Q4/Q5 先做；Q6 按当前题目交互确认目标）
  -> 人工放球到可识别位置，确认视觉不是反光
  -> 仅按一次 SW1
  -> 保存这一轮日志并核对 mode/epoch
  -> 先判断：链路 / 状态机 / 视觉 / 执行器 / 控制 哪一域
  -> 只改一个变量，编译烧录（或空闲时下发 Q3 RAM 参数）
  -> 冷启动复跑，记录最大误差和收敛时间
```

把这套顺序守住，调参就会从“感觉不对一直加参数”变成有证据的闭环。尤其是：先确认输入可信、确认没有饱和，再决定调哪一个增益。
