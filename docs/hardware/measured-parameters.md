# H题实物参数记录

Updated: 2026-07-31

## 钢球

| 参数 | 当前值 | 状态 | 来源 |
|---|---:|---|---|
| 质量 `m` | `4.11 g`（`0.00411 kg`） | 已由用户提供实测值；秤的分辨率待补 | 现场测量 |
| 直径 `D` | 约 `10 mm` | 用户确认约值，尚未用卡尺精测 | 用户确认 |
| 半径 `R` | 约 `5 mm` | 由名义直径推算 | 计算值 |

按实心钢球和名义半径计算：

```text
J = 2/5*m*R^2 = 4.11e-8 kg*m^2
F_g = m*g = 0.04031 N
rolling_factor = 1 / (1 + J/(m*R^2)) = 5/7
```

球位于摆杆中心外 `0.12 m` 和 `0.125 m` 时，水平附近由重力产生的准静态负载力矩分别约为：

```text
tau_0.12  = m*g*0.12  = 4.84 mN*m
tau_0.125 = m*g*0.125 = 5.04 mN*m
```

## 对控制模型的影响

- 在理想纯滚动方程中，质量与半径会约去，因此 `4.11 g` 不改变外层模型的 `5/7` 系数，也不要求仅因质量变化重新计算当前 LQR 增益。
- 质量必须进入摆杆执行器负载校核。角度内环辨识应分别进行空载和带球阶跃，当前仿真的 `25 ms` 时常最终采用带球结果。
- 当前端部碰撞用恢复系数和加速度脉冲近似。碰撞峰值、接触时间和脱轨风险需要用 `4.11 g` 实球的视频/传感记录重新标定。
- 打滑模型需要实测静摩擦和急加速边界。只有确认纯滚动假设成立，`5/7` 模型才可作为正式控制模型。

## RS00两连杆机构

| 参数 | 当前值 | 状态 |
|---|---:|---|
| 水管物理长度 | `250 mm` | 用户给定；不直接等于球心可用行程 |
| 视觉零点到两侧物理挡边 | `112 mm / 112 mm` | 用户确认；物理挡边，不是球心极限 |
| 两物理挡边间距 | `224 mm` | 由两侧112 mm计算 |
| 视觉零点到合页轴心C | `155 mm` | 用户确认；沿水管方向 |
| 水管合页到连接点 `CB` | `300.1 mm` | 用户最终确认 |
| RS00主动曲柄 `OA` | `35.0 mm` | 用户给定 |
| 蓝色连接杆 `AB` | `55.5 mm` | 用户给定 |
| RS00轴心 `O` 高度 | `38 mm` | 用户给定 |
| 水管右侧合页轴心 `C` 高度 | `93 mm` | 用户给定 |
| 固定轴 `O-C` 水平距离 | `285 mm` | 用户给定 |
| 支撑台最低高度 | `50 mm` | 用户最终更正 |
| 水平时 `B-O` 水平偏置 | `-15.1 mm` | B在O左侧 |

C是右侧固定合页，水管水平时`C->B`向左；O位于C左侧285 mm、低55 mm。若水管外径约
50 mm且C轴与水管中心轴重合，管底约68 mm，满足支撑台不低于50 mm的约束。

### 钢球视觉坐标与C轴

统一使用树莓派输出的钢球轴向位置`x_cam`：

```text
x_cam = 0       视觉零点
x_cam < 0       钢球向合页轴心C运动
x_cam > 0       钢球远离合页轴心C运动
x_C = -155 mm   C轴在视觉坐标中的位置
```

钢球球心到C轴沿管方向的距离为`s_C=155 mm+x_cam`。物理挡边坐标为
`[-112,+112] mm`。若钢球半径为`R`，且物理挡边就是掉球边界，则球心几何极限为
`[-112+R,+112-R] mm`。按用户确认的约10 mm直径，即`R≈5 mm`，球心几何极限为
`±107 mm`。钢球直径未精测前不能冻结最终软件限位。合页侧物理挡边
距C轴`43 mm`，远端物理挡边距C轴`267 mm`。

2026-07-31 MATLAB机构分析结果：`+-6 deg`全部可达，RS00相对水平零位约需
`-57.00 deg`到`+70.68 deg`，最小`|dtheta/dq|=0.0467`，最差速度/力矩换算约
`21.43`。当前雅可比保护下连续区约`-6.47 deg`到`+6.79 deg`，因此软件硬指令暂定
`+-6 deg`、恢复角`+-5.5 deg`、水管斜率`0.35 rad/s`。水平RS00编码器原始读数和机械
干涉边界仍要无球标定，不能直接使用理论零偏。

## 钢球—水管接触

当前Simulink起始包络：

```text
mu_static = 0.050
mu_kinetic = 0.030
rolling_resistance = 0.002
```

这些是低摩擦暂定值，不是已完成的材料实测。应在2、3、4、5度斜管条件各重复至少10次，
并用带标记钢球的视频区分纯滚动与滑动。

## 下一步测量

1. 用卡尺沿至少三个方向测钢球直径并记录最小值、最大值和仪器分辨率；当前约值为
   `10 mm`直径、`5 mm`半径。
2. 记录电子秤型号、分辨率及重复称量结果，给出质量不确定度。
3. 比较空载/带球摆杆小角度阶跃，辨识时常、纯延迟、死区和电机电流变化。
4. 用高速视频或带标记钢球测量何时开始打滑，冻结允许的最大摆角速度和加速度。
5. 实测合页轴高、全部孔距、水平RS00编码器角度、装配分支和机械干涉边界。

## 尚未冻结的物理参数

| 优先级 | 参数 | 当前情况 | 影响 |
|---|---|---|---|
| P0 | 钢球精确直径 | 用户确认约10 mm，卡尺值待补 | 球心限位、质量惯量、视觉模板 |
| P0 | `112 mm`测量误差 | 已知为视觉零点到物理挡边，精度未知 | 掉球边界与安全余量 |
| P0 | `155 mm`是否沿水管中心线测量 | 当前按沿管方向处理 | C轴力臂和负载力矩 |
| P0 | 水管截面、内径和材质 | 模型暂按PVC、内径50 mm | 接触几何、摩擦和横向晃动 |
| P0 | 水平时RS00原始编码器角度 | 未测 | 四杆逆解零位与装配分支 |
| P1 | OA/AB/CB/OC孔距测量误差 | 只有标称单值 | 雅可比、死点和±6°工作区 |
| P1 | 管体、支架、主动杆和连杆质量/质心 | 目前是占位值 | RS00负载、阶跃响应和力矩限制 |
| P1 | 静摩擦、动摩擦、滚阻 | 只有压力测试包络 | 钢球加速度、制动距离和控制增益 |
| P1 | RS00小角度响应、回差和反馈频率 | 未做带时间戳阶跃 | 执行延迟、角速度限制和稳定裕度 |
| P1 | IMU型号、安装方向、实际源频率和延迟 | UART为115200，模型按200 Hz/8 ms | 前馈符号、相位和有效性 |
| P1 | 相机动态延迟与像素标定 | 静态重复性已测，动态真值未测 | 卡尔曼延迟补偿和1 cm判据 |
| P2 | 车辆最大加速度、俯仰和坑洼振动 | 目前使用合成工况 | 实际压力测试包线 |
