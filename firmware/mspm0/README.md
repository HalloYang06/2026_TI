# 天猛星 MSPM0G3507 底盘固件

当前实机循迹工程位于
[wit-oled-hardware-spi](wit-oled-hardware-spi/README.md)。

## 当前状态

用户已确定继续使用上一年参赛的天猛星/MSPM0G3507作为底盘控制器，当前小车工程已接近调通。
后续滚球部署不得擅自重构原底盘逻辑。源码可确认WIT使用UART3、RX=`PB3`、TX=`PB2`、
DMA与RX timeout中断，当前SysConfig波特率为`115200 bit/s`；`TIMER_0`为20 ms旧小车PID。
这不能证明IMU或板间状态已经达到500 Hz。

## 计划职责

| 任务 | 频率 | 要求 |
|---|---:|---|
| 红外光电采样 | 1 kHz | 定时器触发；比赛正式循迹只使用允许的红外光电信息 |
| IMU UART DMA接收 | 目标500 Hz | 先实测WIT输出率；二进制帧、校验、采样age |
| 底盘速度/差速环 | 保持已调通配置 | 与滚球算法解耦，不因文档目标频率直接改动 |
| 左右轮编码器/里程计 | 保持已调通配置 | 输出实际轮速和曲率，不只输出目标值 |
| 状态发布 | 200或500 Hz，待接口冻结 | 加速度、俯仰、偏航角速度、轮速、故障、序号、时间、CRC |

滚球KF/LQI不放在MSPM0。MSPM0只保证底盘实时控制和可靠状态采集；
EdgeTalk根据这些状态计算水管角并通过CAN控制RS00。

## IMU串口建议

- MSP到EdgeTalk推荐`460800`或`921600 bit/s`、8N1、UART DMA环形缓冲。
- 当前WIT到MSP是UART3 `115200 bit/s`；它是传感器链路，不等于板间发布链路。
- 传感器链路应先测真实输出帧长、突发间隔和最大解析延迟；理论带宽够不等于控制余量够。
- 加速度和角速度先做坐标系转换、量程检查和抗混叠低通，再按冻结的板间发布频率发包。
- 状态包携带采样时刻而不是发送完成时刻；检测丢帧、乱序、CRC错误和传感器重启。

## 待确认信息

- 天猛星板卡具体版本和 SDK/工程版本。
- IMU型号、协议、量程、内部输出频率和安装方向。
- 红外通道数及ADC/比较器连接。
- 左右电机驱动方式、编码器PPR、轮径、轮距和减速比。
- MSPM0到EdgeTalk物理接口、电平和引脚。
